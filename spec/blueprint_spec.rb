require 'spec_helper'

describe Blueprint do
  it 'has a version number' do
    expect(Blueprint::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
